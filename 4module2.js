const input=require('readline-sync')

const c=input.questionInt('enter the length : ')
let arr=[]
for (let i = 0; i < arr.length; i++) {
    for (let j = 0; j < arr.length; j++) {
        arr[i]=input.question('enter the key : ')
        arr[j]=input.questionInt('enter the elements : ')
        
    }
    
}